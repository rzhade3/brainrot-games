import PlaceholderScene from '../../core/PlaceholderScene';

/** Stub scene for Tower Defense — replace with real gameplay later. */
export default class TowerDefenseScene extends PlaceholderScene {
  init(): void {
    super.init({ title: 'Tower Defense', emoji: '🏰' });
  }
}
