import PlaceholderScene from '../../core/PlaceholderScene';

/** Stub scene for the Top-Down game — replace with real gameplay later. */
export default class TopDownScene extends PlaceholderScene {
  init(): void {
    super.init({ title: 'Top Down', emoji: '🕹️' });
  }
}
